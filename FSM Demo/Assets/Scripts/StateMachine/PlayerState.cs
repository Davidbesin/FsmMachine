public enum MovementStateType
{
    Idle,
    Move,
    Jump,
    Fall,
    Dash
}

public enum ActionStateType
{
    None,
    Attack,
    ChargeAttack,
    Interact
}

public enum StatusStateType
{
    Normal,
    Hurt,
    Dead
}